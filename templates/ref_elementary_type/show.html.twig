{% extends 'base.html.twig' %}

{% block title %}RefElementaryType{% endblock %}

{% block body %}
    <h1>RefElementaryType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ref_elementary_type.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ ref_elementary_type.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ref_elementary_type_index') }}">back to list</a>

    <a href="{{ path('ref_elementary_type_edit', {'id': ref_elementary_type.id}) }}">edit</a>

    {{ include('ref_elementary_type/_delete_form.html.twig') }}
{% endblock %}
