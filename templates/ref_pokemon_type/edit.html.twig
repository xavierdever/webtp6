{% extends 'base.html.twig' %}

{% block title %}Edit RefPokemonType{% endblock %}

{% block body %}
    <h1>Edit RefPokemonType</h1>

    {{ include('ref_pokemon_type/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('ref_pokemon_type/_delete_form.html.twig') }}

    <div>
        <button type="button" onclick="location.href='{{ path('ref_pokemon_type_index') }}'" class="btn btn-primary btn-lg">Retour aux espèces</button>
    </div>
{% endblock %}
